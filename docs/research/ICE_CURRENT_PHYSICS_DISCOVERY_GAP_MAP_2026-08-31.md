# ICE current physics-discovery gap map

> Status: repository-local ontology navigation for the computational workbench.
> This is not a physics claim, a research contract, an execution order, or an
> external-KG promotion.  Raw result files remain the check-ledger sources of
> truth.

## Short answer

The repository contains new **scoped computational facts**, but it does not yet
contain a new-physics discovery.  Each active lane currently stops at a local
lemma, convention, or precondition.  None reaches the full chain

```text
model-specific certificate
  -> globally defined constraint/operator construction
  -> gauge/constraint consistency
  -> physical product, state and observable
  -> convention-stable prediction
  -> comparison with data and alternatives.
```

Failure to cross that chain is an epistemic boundary: it does not prove that
new physics is absent.

## Collection-wide context

The same boundary appears in each independent repository graph, for different
reasons. They must not be merged into one evidential chain.

| Graph | What survives | Why it is not new physics |
| --- | --- | --- |
| Hypercomplex | Selected finite algebraic identities and subgroup/orbit calculations. | The proposed Standard-Model referents and an algebra-to-observable discriminator are not established. |
| Legacy predictions | Narrative and historical numerical lists are preserved. | The stored genuine-signal gates fail, and the lists lack independent prior Git timestamps for the claimed comparisons. |
| IG-RUEQFT locality | One finite free-U(1) charge-dephasing oracle is reproducible. | Its registered bulk-locality shortcut is contradicted on the sampled system; the interacting continuum discriminator remains open. |
| CPT temporal-folded SUSY / Gate 1 | The scoped raw-C, closed-S3, selected-H and finite BFV facts summarized below. | The operator, constraint, state, observable and empirical bridges remain incomplete. |

Negative and null results narrow the model space, but proximity of these four
graphs is not mutual confirmation.

## Current fact / interpretation / open-bridge separation

| Lane | Computed fact | Allowed interpretation | Missing bridge before promotion |
| --- | --- | --- | --- |
| P1 real raw-C | On root bracket 1 and both punctured real lambda boxes, the actual plus-recessive entering sensitivity at Q=4 obeys `27/10 <= s(4) <= 240/53`. A Bessel-preconditioned 512/1024-kernel-panel comparison then encloses both the actual sensitivity and mean-value quotient at `Q_switch=-29/10` in `[0.08636945814095967975, 0.14690505220523634520]`. The negative/positive-lambda actual directions lie strictly below/above the lambda-zero Bessel direction, with widths below `1e-4`. | The entering sensitivity remains positive through the switch, and common-direction wrapping has been removed for this finite Bessel-relative endpoint comparison. This is not a panel-specific absolute-rho Picard tube or a Gamma_1 result. | Panel-specific absolute-rho Picard tubes where required; differentiated node-safe transfer from `Q_switch` to `Q0=-4`; the actual declared Gamma_1 value including the complete minus-tail remainder; sign/zero separation; root continuation and velocity. |
| P4 nonreal raw-C | One fixed upper-half-plane box is separated from the principal square-root cut and has a positive real-part lower bound. | The chosen branch is safe for a later complex-tail construction on that box. | A normalization-fixed complex-tail/Volterra certificate for the actual recessive endpoint; selected-extension singular Weyl m(z); boundary values and spectral measure/multiplicity; raw-C test space, rigging form and RAQ; raw-C/H comparison. |
| P2--P3 closed S3 | The n=1 scalar-derived tracefree Hessian vanishes, so no independent E1 shear pair is admitted.  One selected Q2 metric-plus-matter DH packet cancels the separately identified fixed-metric strain at L=2 and L=3, with the L=2 omitted k=3 remainder retained. | These results fix one exceptional-mode convention and one local covariance-consistency packet. | Complete scalar/vector/TT basis and derivative/Gaunt data; the gravitational Hamiltonian constraint and its metric variation; cubic constraints; combined DD, general DH and full HH; a projection-remainder theorem; projected Jacobi closure. |
| P5 quantum BFV | No new P5 result follows from the scoped Q2 packet. | Quantum work remains downstream of actual classical structure functions and domains. | A regulated classical BFV charge; compatible regulator and cutoff bookkeeping; a common invariant operator core; an operator-level nilpotency/anomaly audit. |
| P6 boundary/BFV | Relative finite-dimensional orientation and toy gluing controls exist elsewhere in the graph. | They calibrate conventions only. | Physical endpoint polarization, relative-homology lapse cycle, continuum determinant/Pfaffian line, two-slab gravity gluing and an absolute measure. |
| P7 physical/empirical | No likelihood is authorized. | A number from a frozen V=0 or conditional background is not yet an observable prediction. | Physical product, clock, normalized state, V!=0 perturbation-generation model, reheating/pivot map, normalization and a comparison-ready observable before CLASS/Cobaya likelihood. |

## Dependency spine

The active logical dependencies are:

```text
P1: Q=4 s anchor
  -> Bessel-relative positive-sensitivity and side certificate at Q_switch
  -> panel-specific rho control + differentiated Q_switch-to-Q0 transfer
  -> actual Gamma_1/minus-tail
  -> sign/root continuation

P4: nonreal branch precondition
  -> actual recessive endpoint certificate
  -> selected-extension m(z)
  -> spectral measure/multiplicity
  -> raw-C RAQ and C/H comparison

P2/P3: n=1 convention + selected Q2 DH packet
  -> full SVT/cubic constraints
  -> DD + general DH + full HH + projected Jacobi

P3 + P4 + P6
  -> classical/quantum BFV with a common operator core
  -> physical product + clock + normalized state
  -> V!=0 generation + reheating + observable normalization
  -> likelihood and alternative-model comparison.
```

The arrows are prerequisites, not automatic successors.  One successful node
does not authorize or generate the next calculation.

## Why discovery is hard here

1. **The present results are local.**  A fixed parameter box, one harmonic
   packet, or one cutoff identity cannot establish a global operator, algebra,
   state, or observable.
2. **Gauge consistency is conjunctive.**  One correct DH packet does not imply
   DD, HH, Jacobi, BFV nilpotency, or anomaly freedom.
3. **Quantization adds choices.**  Extension data, ordering, domains, test
   spaces and measures are not fixed by a classical identity.  The selected-H
   RAQ construction cannot be silently reused as raw-C RAQ.
4. **A physical prediction needs a state and an observable.**  Until the
   physical product, clock, state, reheating transfer and normalization are
   fixed, a likelihood would compare an underdefined quantity.
5. **Computational novelty is not yet scientific novelty.**  A repository-local
   exact identity becomes a discovery only after its scope is shown to be
   nontrivial relative to established theory, robust to conventions and
   independent checks, and capable of changing a theoretical or empirical
   conclusion.

## Promotion rule

Call a result a new theoretical-physics finding only when it changes a
well-defined model-level claim after all required domain, gauge and scope
conditions are discharged.  Call it an empirical discovery only after a
normalized observable has a preregistered comparison against data and viable
alternatives.  Until then use the narrower labels already in the graph:
`computed fact`, `scoped interpretation`, `open problem`, or `precondition`.

The current honest status is therefore:

```text
COMPUTATIONAL_PROGRESS
NO_PHYSICS_DISCOVERY_ESTABLISHED
SCIENTIFIC_ROUTE_OPEN
GLOBAL_PROMOTION_PROHIBITED
```

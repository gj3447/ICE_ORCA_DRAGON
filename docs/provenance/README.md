# Provenance

[`SOURCES.md`](SOURCES.md) preserves the repository's source and mythology/physics provenance. The
current runnable catalog is not inferred from that historical corpus; use `./ice list --json`.
